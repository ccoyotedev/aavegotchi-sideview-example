# Aavegotchi sideviews

A simple example using both `getAavegotchiSideSvgs` and `previewSideAavegotchi` methods.

## To run example

- Clone repo
- `yarn install` to install all dependencies
- Ensure you are on the Matic network
